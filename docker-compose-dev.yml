version: '3.9'


services:

  gtaconnected:

    image: mrose5736/gtaconnected-docker:latest

    container_name: gta-connected-server

    restart: unless-stopped
    ports:

      - 22000:22000/tcp
      - 22000:22000/udp